<adonis:AdonisWindow x:Class="TemplateApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:adonis="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        xmlns:s="https://github.com/canton7/Stylet"
        mc:Ignorable="d"
        Title="MainView" Height="350" Width="500">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Label Content="{Binding SubItemTitle}" Margin="0 0 8 0"/>
            <Label Content="{Binding Number}" DockPanel.Dock="Top"/>
        </StackPanel>
        <DockPanel DockPanel.Dock="Bottom" LastChildFill="False">
            <Button Command="{s:Action Previous}" DockPanel.Dock="Left">Previous</Button>
            <Button Command="{s:Action Next}" DockPanel.Dock="Right">Next</Button>
        </DockPanel>
        <ContentControl s:View.Model="{Binding ActiveItem}"/>
    </DockPanel>
</adonis:AdonisWindow>
